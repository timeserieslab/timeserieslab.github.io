{
  "private": true,
  "scripts": {
    "build-preview": "pulp build --skip-entry-point -m PreviewMain --to scripts/preview.js",
    "watch-preview": "pulp -w build --skip-entry-point -m PreviewMain --to scripts/preview.js"
  },
  "devDependencies": {
    "pulp": "^10.0.1",
    "purescript": "^0.10.7",
    "purescript-psa": "^0.4.0"
  }
}
