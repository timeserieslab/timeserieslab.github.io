{
  "private": true,
  "scripts": {
    "build-preview": "pulp build -m PreviewMain --to scripts/preview.js",
    "watch-preview": "pulp -w build -m PreviewMain --to scripts/preview.js",
    "build-sandbox": "pulp build -m SandboxMain --to scripts/sandbox.js",
    "watch-sandbox": "pulp -w build -m SandboxMain --to scripts/sandbox.js"
  },
  "devDependencies": {
    "pulp": "^10.0.1",
    "purescript": "^0.10.7",
    "purescript-psa": "^0.4.0"
  }
}
